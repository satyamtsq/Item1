name: GitHub Actions Pipelinee
# gopal2
on:
 push:
   branches:
     - main
jobs:
 invoke_api:
   runs-on: windows-latest
   steps:
     - name: Call REST API
       run: |
        $BatchscheduleID = '${{ vars.BATCHSCHEDULEID }}'
         $params = @{
           Uri = 'http://172.31.19.121:8080/invokeFromPipeline'
           Method = 'POST'
           Body = @{
             Usernameofbreez = 'qa@chartrequest.com'
             Passwordofbreez = 'tsq@123'
             BatchscheduleID = $BatchscheduleID
             IpAddress = 'http://172.31.19.121:8080/'
           }
         }
         Invoke-RestMethod @params
